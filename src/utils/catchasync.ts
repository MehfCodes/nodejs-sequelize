// import { NextFunction } from 'express';

// export const catchAsync = (fn: <T>) :<T> => {
//   return (req: Request, res: Response, next: NextFunction):<T> =>
//     fn(req, res, next).catch(next); //equla to : .catch(err=>next(err))
// };
